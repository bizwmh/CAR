additive = false
appender = car.logger.rt.appender
file = RTLogger.txt
logger = car.logger.rt
path = log
pattern = "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
timestamp = true